Thư mục này chứa các file cài đặt Python:
- python-3.11.9-embed-amd64.zip : Python 3.11 portable (tải về khi chạy build_dist.ps1)
- get-pip.py                    : Bootstrap pip (tải về khi chạy build_dist.ps1)

Nếu chạy install.bat mà thiếu 2 file trên, install.bat sẽ tự tải về.
